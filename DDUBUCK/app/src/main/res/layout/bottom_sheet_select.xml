<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:orientation="vertical"
    android:id="@+id/bottom_sheet_select"
    android:layout_width="match_parent"
    android:layout_height="match_parent">
    <TextView
        android:id="@+id/home_bottom_title"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginStart="16dp"
        android:layout_marginTop="30dp"
        android:text="경로 선택하기"
        android:textColor="@color/black"
        android:textSize="18sp"
        android:textStyle="bold" />
    <!--
    여기서 Recycler View 를 생성하면서
    adapter 가 부착되지 않았다는 로그가 발생하는 것 같습니다
    (No adapter attached; skipping layout)
    BottomSheetSelectFragment.kt 파일에서 초기화를 정상적으로 진행하니
    해당 로그는 무시해도 좋습니다.
    -->
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/sheet_recycler"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:overScrollMode="never"
        app:layoutManager="androidx.recyclerview.widget.LinearLayoutManager"
        tools:listitem="@layout/bottom_sheet_select_item">
    </androidx.recyclerview.widget.RecyclerView>
</LinearLayout>